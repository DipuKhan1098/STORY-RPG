/**
 * battleNodes.js
 * PURPOSE:
 *  - CRUD for battleNodes.json
 *  - Used by Admin to configure battles separately from storyNodes
 *  - Game fetches on battle start
 *
 * GAME:
 *  GET /battleNode/:id → Returns enemies, environment, rewards, nextNodeIds
 */
