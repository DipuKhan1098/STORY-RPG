/**
 * classes.ts
 * PURPOSE:
 *  - CRUD for player classes.
 *
 * FUNCTIONS:
 *  - listClasses(): Promise<Class[]>
 *  - getClass(id: string): Promise<Class>
 *  - createClass(data: Partial<Class>): Promise<Class>                  // admin-only
 *  - updateClass(id: string, data: Partial<Class>): Promise<Class>      // admin-only
 *  - deleteClass(id: string): Promise<void>                             // admin-only
 */
