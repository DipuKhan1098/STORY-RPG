/**
 * Continue.tsx
 * PURPOSE:
 *  - (Future) List saved players from players.json and allow resuming.
 *  - For now: this route/component is a placeholder and should be hidden in UI.
 *
 * FUTURE DATA:
 *  - GET /api/players → show saved profiles (name, level, race, class)
 *  - Click → set active player in context and navigate to /player/game
 *
 * TRY→ALERT:
 *  - If loading fails, alert and remain on page.
 */
